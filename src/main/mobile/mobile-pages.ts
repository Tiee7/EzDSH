import { icon, type IconDefinition } from '@fortawesome/fontawesome-svg-core'
import {
  faArrowLeft,
  faArrowUp,
  faBrain,
  faCheck,
  faChevronDown,
  faChevronRight,
  faComment,
  faPlus,
  faRotate,
  faStop,
  faTerminal,
} from '@fortawesome/free-solid-svg-icons'

export type MobilePageLocale = 'zh' | 'en'

export interface MobilePageOptions {
  locale: MobilePageLocale
}

const ICONS = {
  arrowLeft: renderIcon(faArrowLeft),
  arrowUp: renderIcon(faArrowUp),
  brain: renderIcon(faBrain),
  check: renderIcon(faCheck),
  chevronDown: renderIcon(faChevronDown),
  chevronRight: renderIcon(faChevronRight),
  comment: renderIcon(faComment),
  plus: renderIcon(faPlus),
  rotate: renderIcon(faRotate),
  stop: renderIcon(faStop),
  terminal: renderIcon(faTerminal),
}

export function renderMobilePage({ locale }: MobilePageOptions): string {
  const zh = locale === 'zh'
  const copy = {
    title: zh ? 'EzDSH 手机端' : 'EzDSH Mobile',
    connected: zh ? '已连接' : 'Connected',
    reconnecting: zh ? '正在重新连接' : 'Reconnecting',
    disconnected: zh ? '连接已断开' : 'Disconnected',
    sessions: zh ? '会话' : 'Sessions',
    mobileWorkspace: zh ? '移动工作区' : 'Mobile workspace',
    newSession: zh ? '新建' : 'New',
    workspace: zh ? '工作区' : 'Workspace',
    chooseWorkspace: zh ? '选择工作区' : 'Choose a workspace',
    recentSessions: zh ? '最近会话' : 'Recent sessions',
    noWorkspace: zh ? '还没有可用工作区，请先在电脑端添加一个。' : 'No workspace is available. Add one from the desktop first.',
    noSessions: zh ? '这里还没有会话' : 'No sessions yet',
    noSessionsHint: zh ? '新建一个会话，或切换到其他工作区。' : 'Start a session or switch to another workspace.',
    untitled: zh ? '未命名会话' : 'Untitled session',
    justNow: zh ? '刚刚' : 'Just now',
    minute: zh ? ' 分钟前' : 'm ago',
    hour: zh ? ' 小时前' : 'h ago',
    day: zh ? ' 天前' : 'd ago',
    back: zh ? '返回会话' : 'Back to sessions',
    model: zh ? '模型' : 'Model',
    modelDefault: zh ? '默认模型' : 'Default model',
    modelCurrent: zh ? '当前模型' : 'Current model',
    modelLoadHint: zh ? '点击后加载可用模型' : 'Tap to load available models',
    modelLoading: zh ? '正在加载模型…' : 'Loading models…',
    modelLoadingFailed: zh ? '模型列表加载失败，点击重试' : 'Could not load models. Tap to retry.',
    modelEmpty: zh ? '当前没有可切换的模型' : 'No switchable models are available',
    modelSelect: zh ? '选择模型' : 'Choose a model',
    modelSelected: zh ? '已切换模型' : 'Model switched',
    emptyConversation: zh ? '告诉 Agent 你想完成什么。' : 'Tell the Agent what you want to accomplish.',
    prompt: zh ? '给 Agent 发送消息' : 'Message the Agent',
    send: zh ? '发送' : 'Send',
    stop: zh ? '停止' : 'Stop',
    running: zh ? '正在执行' : 'Working',
    thinking: zh ? '思考' : 'Think',
    toolRunning: zh ? '运行中' : 'Running',
    toolDone: zh ? '已完成' : 'Done',
    toolFailed: zh ? '失败' : 'Failed',
    input: zh ? '输入' : 'Input',
    output: zh ? '输出' : 'Output',
    refreshed: zh ? '已刷新' : 'Refreshed',
    requestFailed: zh ? '请求失败' : 'Request failed',
    connectionLost: zh ? '手机连接已失效，请重新配对。' : 'This phone connection expired. Pair the phone again.',
  }

  return `<!doctype html>
<html lang="${zh ? 'zh-CN' : 'en'}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,viewport-fit=cover">
  <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme:light)">
  <meta name="theme-color" content="#141416" media="(prefers-color-scheme:dark)">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-title" content="EzDSH">
  <link rel="icon" href="/app-icon">
  <link rel="apple-touch-icon" href="/app-icon">
  <title>${copy.title}</title>
  <style>
    :root{color-scheme:light;--paper:#fff;--surface:#f6f7f8;--surface-strong:#eef0f2;--card:#fff;--ink:#18191c;--muted:#858990;--line:#e5e7eb;--brand:#4d6bfe;--danger:#df4754;--success:#35a867;--shadow:0 14px 38px rgba(22,25,31,.08)}
    @media(prefers-color-scheme:dark){:root{color-scheme:dark;--paper:#141416;--surface:#202023;--surface-strong:#29292d;--card:#1d1d20;--ink:#f5f5f6;--muted:#9799a0;--line:#303034;--brand:#7188ff;--danger:#ff6b78;--success:#5fc282;--shadow:0 16px 42px rgba(0,0,0,.28)}}
    *{box-sizing:border-box}html,body{width:100%;height:100%;overflow:hidden}body{margin:0;background:var(--paper);color:var(--ink);font:14px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;-webkit-font-smoothing:antialiased}button,textarea,select{font:inherit}button{color:inherit;cursor:pointer}button:focus-visible,select:focus-visible,textarea:focus-visible{outline:2px solid color-mix(in srgb,var(--brand) 72%,transparent);outline-offset:2px}button:disabled{cursor:default;opacity:.45}[hidden]{display:none!important}
    .shell{width:min(760px,100%);height:var(--app-height,100dvh);margin:0 auto;padding:calc(6px + env(safe-area-inset-top)) 14px calc(8px + env(safe-area-inset-bottom));display:flex;flex-direction:column;overflow:hidden}.app-header{height:48px;display:flex;flex:none;align-items:center;justify-content:space-between;padding:0 3px}.brand{display:flex;align-items:center;gap:9px;min-width:0;font-weight:650;letter-spacing:-.015em}.brand img{width:26px;height:26px;border-radius:7px;object-fit:cover}.connection-state{display:inline-flex;align-items:center;gap:7px;color:var(--muted);font-size:11px}.connection-dot{width:8px;height:8px;border-radius:999px;background:var(--success);box-shadow:0 0 0 0 color-mix(in srgb,var(--success) 42%,transparent);animation:connected-pulse 1.8s ease-out infinite}.connection-state.connecting .connection-dot{background:var(--muted);animation:none}.connection-state.error .connection-dot{background:var(--danger);animation:none}@keyframes connected-pulse{70%{box-shadow:0 0 0 7px transparent}}
    .view{display:none;min-height:0}.view.active{display:flex;flex:1;flex-direction:column}.session-hero{display:flex;align-items:flex-end;justify-content:space-between;gap:14px;padding:9px 2px 13px}.eyebrow{margin:0 0 2px;color:var(--muted);font-size:10px;font-weight:650;letter-spacing:.09em;text-transform:uppercase}.session-hero h1{margin:0;font-size:27px;line-height:1.12;font-weight:680;letter-spacing:-.04em}.action-button{min-height:38px;border:1px solid var(--line);border-radius:12px;padding:0 13px;display:inline-flex;align-items:center;justify-content:center;gap:7px;background:var(--card);font-weight:620}.action-button.primary{border-color:var(--ink);background:var(--ink);color:var(--paper)}.action-button:hover:not(:disabled){transform:translateY(-1px)}.action-button svg{width:13px;height:13px}
    .workspace-panel{flex:none;padding:9px;border:1px solid var(--line);border-radius:15px;background:var(--surface)}.workspace-label{display:block;margin:0 3px 5px;color:var(--muted);font-size:10px;font-weight:650;letter-spacing:.08em;text-transform:uppercase}.workspace-controls{display:flex;gap:8px}.workspace-controls select{min-width:0;flex:1;height:41px;border:1px solid var(--line);border-radius:11px;padding:0 12px;background:var(--card);color:var(--ink);font-weight:520}.icon-button{width:41px;height:41px;flex:none;display:grid;place-items:center;border:1px solid var(--line);border-radius:11px;padding:0;background:var(--card)}.icon-button svg{width:14px;height:14px}.icon-button.refreshing svg{animation:spin .65s linear infinite}.workspace-hint{margin:7px 3px 0;color:var(--muted);font-size:12px}.list-heading{display:flex;align-items:center;justify-content:space-between;padding:15px 2px 7px}.list-heading strong{font-size:13px;font-weight:650}.session-count{min-width:25px;height:20px;padding:0 7px;display:grid;place-items:center;border-radius:999px;background:var(--surface);color:var(--muted);font-size:11px;font-variant-numeric:tabular-nums}
    .session-list{min-height:0;display:grid;align-content:start;gap:8px;overflow-y:auto;padding-bottom:12px;-webkit-overflow-scrolling:touch}.session-row{width:100%;min-width:0;display:flex;align-items:center;gap:11px;border:1px solid var(--line);border-radius:15px;padding:12px;background:var(--card);text-align:left;transition:background .15s ease,transform .15s ease}.session-row:hover,.session-row:active{background:var(--surface);transform:translateY(-1px)}.session-mark{width:35px;height:35px;flex:none;display:grid;place-items:center;border-radius:11px;background:var(--surface)}.session-mark svg{width:15px;height:15px}.session-copy{min-width:0;flex:1}.session-copy strong{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-weight:610}.session-copy time{display:block;margin-top:2px;color:var(--muted);font-size:11px}.session-chevron{flex:none;color:var(--muted)}.session-chevron svg{width:11px;height:11px}.empty{min-height:210px;padding:42px 20px;display:flex;flex-direction:column;align-items:center;justify-content:center;color:var(--muted);text-align:center}.empty-mark{width:45px;height:45px;margin-bottom:12px;display:grid;place-items:center;border-radius:14px;background:var(--surface);color:var(--ink)}.empty-mark svg{width:17px;height:17px}.empty strong{color:var(--ink)}.empty span:last-child{max-width:240px;margin-top:4px;font-size:12px}.skeleton-list{display:grid;gap:9px}.skeleton{height:60px;border-radius:15px;background:var(--surface);animation:pulse 1s ease-in-out infinite alternate}
    body.chat-open .shell{padding:calc(6px + env(safe-area-inset-top)) 14px 0}#chatView{position:relative}.chat-toolbar{height:48px;flex:none;display:flex;align-items:center;gap:9px;border-bottom:1px solid color-mix(in srgb,var(--line) 70%,transparent)}.back-button{width:38px;height:38px;display:grid;place-items:center;flex:none;border:0;border-radius:50%;padding:0;background:transparent;color:var(--ink)}.back-button:hover{background:var(--surface)}.back-button svg{width:14px;height:14px}.chat-title{min-width:0;flex:1;text-align:left}.chat-title strong{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:13px;font-weight:640}.chat-title span{display:block;color:var(--muted);font-size:10px}.model-trigger{min-width:0;max-width:132px;height:34px;display:inline-flex;align-items:center;gap:5px;border:1px solid var(--line);border-radius:11px;padding:0 9px;background:var(--surface);font-size:11px;font-weight:620}.model-trigger:hover{background:var(--surface-strong)}.model-trigger-label{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.model-trigger svg{width:10px;height:10px;flex:none;color:var(--muted);transition:transform .15s ease}.model-trigger[aria-expanded=true] svg{transform:rotate(180deg)}.model-menu{flex:none;max-height:min(44vh,360px);margin:10px 0 2px;padding:10px;border:1px solid var(--line);border-radius:15px;background:var(--surface);box-shadow:var(--shadow);overflow-y:auto;-webkit-overflow-scrolling:touch}.model-menu-head{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:1px 3px 8px}.model-menu-head strong{font-size:13px;font-weight:650}.model-menu-head span{display:block;margin-top:1px;color:var(--muted);font-size:11px}.model-menu-state{padding:14px 8px;color:var(--muted);font-size:12px;text-align:center}.model-menu-state.error{color:var(--danger);cursor:pointer}.model-group+.model-group{margin-top:10px}.model-group-name{display:block;padding:6px 7px 4px;color:var(--muted);font-size:10px;font-weight:700;letter-spacing:.08em;text-transform:uppercase}.model-option{width:100%;min-height:48px;display:flex;align-items:center;gap:9px;border:1px solid transparent;border-radius:11px;padding:7px 9px;background:transparent;text-align:left}.model-option:hover{background:var(--card)}.model-option.selected{border-color:color-mix(in srgb,var(--brand) 30%,var(--line));background:color-mix(in srgb,var(--brand) 9%,var(--card))}.model-option-icon{width:25px;height:25px;flex:none;display:grid;place-items:center;border-radius:8px;background:var(--card);color:var(--muted)}.model-option-icon svg{width:11px;height:11px}.model-option-copy{min-width:0;flex:1}.model-option-name{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px;font-weight:620}.model-option-id{display:block;overflow:hidden;color:var(--muted);text-overflow:ellipsis;white-space:nowrap;font:10px ui-monospace,SFMono-Regular,Menlo,monospace}.model-option-check{width:14px;height:14px;display:grid;place-items:center;flex:none;color:var(--brand);opacity:0}.model-option.selected .model-option-check{opacity:1}.model-option-check svg{width:12px;height:12px}
    .messages{flex:1;min-height:0;overflow-y:auto;display:flex;flex-direction:column;padding:18px 4px 94px;-webkit-overflow-scrolling:touch}.message{max-width:100%;color:var(--ink);line-height:1.65;overflow-wrap:anywhere}.message.user{align-self:flex-end;max-width:84%;margin:8px 0 15px;padding:9px 15px;border-radius:21px;background:var(--surface-strong);white-space:pre-wrap}.message.assistant{align-self:stretch;margin:0;padding:0 4px}.markdown{white-space:normal}.markdown>:first-child{margin-top:0}.markdown>:last-child{margin-bottom:0}.markdown p{margin:0 0 10px}.markdown h1,.markdown h2,.markdown h3{margin:18px 0 8px;line-height:1.3}.markdown h1{font-size:21px}.markdown h2{font-size:18px}.markdown h3{font-size:16px}.markdown ul{margin:8px 0;padding-left:22px}.markdown pre{max-width:100%;overflow:auto;margin:9px 0;padding:12px;border:1px solid var(--line);border-radius:11px;background:var(--surface);font:12px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace}.markdown code{border-radius:5px;padding:2px 5px;background:var(--surface);font:12px ui-monospace,SFMono-Regular,Menlo,monospace}.markdown pre code{padding:0;background:transparent}.markdown a{color:var(--brand)}
    .activity{margin:0;white-space:normal}.activity summary{min-height:34px;display:flex;align-items:center;overflow:hidden;padding:5px 2px;border-radius:7px;cursor:pointer;list-style:none}.activity summary::-webkit-details-marker{display:none}.activity summary:hover{background:var(--surface)}.activity-icon{width:16px;height:16px;margin-right:7px;display:grid;place-items:center;flex:none;color:var(--muted)}.activity-icon svg{width:13px;height:13px}.activity-title{flex:none}.activity-dot{width:2px;height:2px;margin:0 8px;flex:none;border-radius:50%;background:var(--muted)}.activity-summary{min-width:0;flex:1;overflow:hidden;color:var(--muted);text-overflow:ellipsis;white-space:nowrap}.activity-chevron{margin-left:6px;color:var(--muted);transition:transform .14s ease}.activity-chevron svg{width:9px;height:9px}.activity[open] .activity-chevron{transform:rotate(90deg)}.activity-body{padding:3px 0 8px 23px;color:var(--muted);font-size:12px}.activity-body strong{display:block;margin:7px 0 3px;color:var(--ink);font-weight:550}.activity-body pre{max-height:260px;overflow:auto;margin:0 0 5px;padding:10px 11px;border:1px solid var(--line);border-radius:10px;background:var(--surface);color:var(--ink);white-space:pre-wrap;font:12px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace}.activity[data-state=error] .activity-icon,.activity[data-state=error] .activity-summary{color:var(--danger)}.streaming::after{content:'';display:inline-block;width:6px;height:15px;margin-left:3px;vertical-align:-2px;background:var(--brand);animation:pulse .7s infinite alternate}.turn-status{height:28px;display:inline-flex;align-items:center;margin:3px 4px 7px;color:var(--brand);font-size:13px;font-weight:620;animation:pulse .9s ease-in-out infinite alternate}
    .chat-error{position:absolute;left:8px;right:8px;bottom:80px;z-index:3;padding:9px 11px;border:1px solid color-mix(in srgb,var(--danger) 35%,var(--line));border-radius:11px;background:var(--card);color:var(--danger);font-size:12px;box-shadow:var(--shadow)}.composer{position:absolute;inset:auto 0 0;z-index:2;padding:10px 0 5px;pointer-events:none}.composer-inner{min-height:58px;display:flex;align-items:flex-end;gap:8px;padding:8px 8px 8px 16px;border:1px solid var(--line);border-radius:23px;background:var(--card);box-shadow:var(--shadow);pointer-events:auto;transition:border-color .15s ease,transform .15s ease}.composer-inner:focus-within{border-color:color-mix(in srgb,var(--ink) 30%,var(--line));transform:translateY(-1px)}.composer textarea{height:40px;min-height:40px;max-height:150px;flex:1;resize:none;overflow-y:auto;border:0;outline:0;padding:9px 0;background:transparent;color:var(--ink);font-size:16px;line-height:22px}.composer textarea::placeholder{color:var(--muted);opacity:.72}.composer-button{width:40px;height:40px;min-width:40px;display:grid;place-items:center;border:1px solid var(--ink);border-radius:50%;padding:0;background:var(--ink);color:var(--paper)}.composer-button svg{width:13px;height:13px}.composer-button.stop{border-color:color-mix(in srgb,var(--danger) 35%,var(--line));background:color-mix(in srgb,var(--danger) 10%,var(--card));color:var(--danger)}.toast{position:fixed;left:50%;top:calc(60px + env(safe-area-inset-top));z-index:6;transform:translate(-50%,-8px);padding:7px 12px;border-radius:999px;background:var(--ink);color:var(--paper);font-size:12px;opacity:0;pointer-events:none;transition:.18s ease}.toast.show{transform:translate(-50%,0);opacity:1}@keyframes pulse{to{opacity:.35}}@keyframes spin{to{transform:rotate(360deg)}}@media(display-mode:standalone){.composer{padding-bottom:calc(5px + env(safe-area-inset-bottom))}.messages{padding-bottom:calc(94px + env(safe-area-inset-bottom))}}@media(prefers-reduced-motion:reduce){*,*::before,*::after{animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important}}
  </style>
</head>
<body>
  <main class="shell">
    <header class="app-header"><div class="brand"><img src="/app-icon" alt=""><span>EzDSH</span></div><div id="connectionState" class="connection-state connecting" role="status"><span class="connection-dot"></span><span>${copy.reconnecting}</span></div></header>
    <section id="sessionsView" class="view active" aria-label="${copy.sessions}">
      <div class="session-hero"><div><p class="eyebrow">${copy.mobileWorkspace}</p><h1>${copy.sessions}</h1></div><button id="newSession" class="action-button primary" type="button">${ICONS.plus}<span>${copy.newSession}</span></button></div>
      <div class="workspace-panel"><label class="workspace-label" for="workspace">${copy.workspace}</label><div class="workspace-controls"><select id="workspace" aria-label="${copy.chooseWorkspace}"><option value="">${copy.chooseWorkspace}</option></select><button id="refresh" class="icon-button" type="button" aria-label="${copy.refreshed}">${ICONS.rotate}</button></div><p id="workspaceHint" class="workspace-hint" hidden></p></div>
      <div class="list-heading"><strong>${copy.recentSessions}</strong><span id="sessionCount" class="session-count">0</span></div>
      <div id="sessionList" class="session-list"><div class="skeleton-list"><div class="skeleton"></div><div class="skeleton"></div><div class="skeleton"></div></div></div>
    </section>
    <section id="chatView" class="view" aria-label="${copy.sessions}">
      <div class="chat-toolbar"><button id="back" class="back-button" type="button" aria-label="${copy.back}">${ICONS.arrowLeft}</button><div class="chat-title"><strong id="chatTitle">${copy.untitled}</strong><span id="chatState">${copy.connected}</span></div><button id="modelTrigger" class="model-trigger" type="button" aria-expanded="false" aria-controls="modelMenu"><span id="modelTriggerLabel" class="model-trigger-label">${copy.modelDefault}</span>${ICONS.chevronDown}</button></div>
      <div id="modelMenu" class="model-menu" hidden><div class="model-menu-head"><div><strong>${copy.modelSelect}</strong><span id="modelMenuHint">${copy.modelLoadHint}</span></div></div><div id="modelOptions"></div></div>
      <div id="messages" class="messages"><div class="skeleton-list"><div class="skeleton"></div><div class="skeleton"></div></div></div>
      <div id="chatError" class="chat-error" hidden></div>
      <form id="composer" class="composer"><div class="composer-inner"><textarea id="prompt" rows="1" maxlength="20000" placeholder="${copy.prompt}" aria-label="${copy.prompt}"></textarea><button id="send" class="composer-button" type="submit" aria-label="${copy.send}">${ICONS.arrowUp}</button><button id="cancel" class="composer-button stop" type="button" aria-label="${copy.stop}" hidden>${ICONS.stop}</button></div></form>
    </section>
  </main>
  <div id="toast" class="toast" role="status"></div>
  <script>
    const C=${safeScriptJson(copy)},I=${safeScriptJson(ICONS)};
    const app={data:null,activeSessionId:null,activeTitle:'',history:{events:[],running:false},optimisticText:'',stream:null,fallbackTimer:null,sending:false,initialized:false,modelDirectory:null,defaultModel:null,modelLoading:false,modelSaving:false,modelError:''};
    const $=id=>document.getElementById(id);
    function esc(value){return String(value??'').replace(/[&<>"']/g,character=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[character]))}
    async function api(path,init){const response=await fetch(path,{credentials:'same-origin',...init,headers:{'Content-Type':'application/json',...(init?.headers||{})}});let payload={};try{payload=await response.json()}catch{}if(response.status===401){location.replace('/');throw new Error(C.connectionLost)}if(!response.ok)throw new Error(payload.error||C.requestFailed+' ('+response.status+')');return payload}
    function setConnection(mode){const host=$('connectionState');host.className='connection-state'+(mode==='connecting'?' connecting':mode==='error'?' error':'');host.lastElementChild.textContent=mode==='connected'?C.connected:mode==='connecting'?C.reconnecting:C.disconnected;$('chatState').textContent=mode==='connected'?(app.history.running?C.running:C.connected):mode==='connecting'?C.reconnecting:C.disconnected}
    function showToast(message){const toast=$('toast');toast.textContent=message;toast.classList.add('show');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>toast.classList.remove('show'),1100)}
    function showChatError(error){const host=$('chatError');host.textContent=error?String(error.message||error):'';host.hidden=!host.textContent}
    function relativeTime(value){const parsed=typeof value==='number'?value:Date.parse(value),elapsed=Math.max(0,Date.now()-(Number.isFinite(parsed)?parsed:Date.now())),minutes=Math.floor(elapsed/60000);if(minutes<1)return C.justNow;if(minutes<60)return minutes+C.minute;const hours=Math.floor(minutes/60);if(hours<24)return hours+C.hour;return Math.floor(hours/24)+C.day}
    function selectedWorkspace(){return app.data?.workspaces?.find(item=>item.workspaceId===$('workspace').value)}
    function sessionsForWorkspace(){const workspace=selectedWorkspace();if(!workspace)return[];const allowed=new Set(workspace.sessionIds||[]);return(app.data?.sessions||[]).filter(item=>allowed.has(item.sessionId)).sort((left,right)=>right.updatedAt-left.updatedAt)}
    function renderSessions(){const workspaces=app.data?.workspaces||[],previous=$('workspace').value;$('workspace').innerHTML=workspaces.length?workspaces.map(item=>'<option value="'+esc(item.workspaceId)+'">'+esc(item.title||item.path)+'</option>').join(''):'<option value="">'+esc(C.chooseWorkspace)+'</option>';$('workspace').value=workspaces.some(item=>item.workspaceId===previous)?previous:(workspaces[0]?.workspaceId||'');renderSessionRows()}
    function renderSessionRows(){const workspace=selectedWorkspace(),items=sessionsForWorkspace();$('newSession').disabled=!workspace;$('workspaceHint').hidden=Boolean(workspace);$('workspaceHint').textContent=workspace?'':C.noWorkspace;$('sessionCount').textContent=String(items.length);$('sessionList').innerHTML=items.length?items.map(item=>'<button class="session-row" type="button" data-session="'+esc(item.sessionId)+'" data-title="'+esc(item.title||C.untitled)+'"><span class="session-mark">'+I.comment+'</span><span class="session-copy"><strong>'+esc(item.title||C.untitled)+'</strong><time>'+esc(relativeTime(item.updatedAt))+'</time></span><span class="session-chevron">'+I.chevronRight+'</span></button>').join(''):'<div class="empty"><span class="empty-mark">'+I.comment+'</span><strong>'+esc(C.noSessions)+'</strong><span>'+esc(C.noSessionsHint)+'</span></div>';document.querySelectorAll('[data-session]').forEach(button=>{button.onclick=()=>openSession(button.dataset.session,button.dataset.title,true)})}
    async function refreshState(options={}){const button=$('refresh');if(options.visible)button.classList.add('refreshing');try{app.data=await api('/api/mobile/state');renderSessions();setConnection(app.data.runtimeReady?'connected':'error');if(!app.initialized&&options.openRecent!==false){app.initialized=true;const recent=[...(app.data.sessions||[])].sort((left,right)=>right.updatedAt-left.updatedAt)[0];if(recent)await openSession(recent.sessionId,recent.title||C.untitled,false)}if(options.visible)showToast(C.refreshed)}catch(error){setConnection('error');if(options.visible)showToast(String(error.message||error))}finally{setTimeout(()=>button.classList.remove('refreshing'),320)}}
    async function createSession(){const workspace=selectedWorkspace();if(!workspace)return;try{const created=await api('/api/mobile/sessions',{method:'POST',body:JSON.stringify({workspaceId:workspace.workspaceId})});await refreshState({openRecent:false});await openSession(created.sessionId,C.untitled,true)}catch(error){showToast(String(error.message||error))}}
    function resetModelPicker(){app.modelDirectory=null;app.defaultModel=null;app.modelLoading=false;app.modelSaving=false;app.modelError='';$('modelMenu').hidden=true;$('modelTrigger').setAttribute('aria-expanded','false');$('modelTriggerLabel').textContent=C.modelDefault;$('modelOptions').innerHTML='';$('modelMenuHint').textContent=C.modelLoadHint}
    function modelKey(selection){return selection?String(selection.provider||'')+'\u0000'+String(selection.model||'')+'\u0000'+String(selection.reasoningEffort||''):''}
    function modelDisplay(selection){if(!selection)return C.modelDefault;const group=(app.modelDirectory?.groups||[]).find(item=>item.id===selection.provider),entry=group?.models?.find(item=>item.id===selection.model);return entry?.name||selection.model||C.modelDefault}
    function modelButton(selection,name,id,selected,isDefault){return '<button class="model-option'+(selected?' selected':'')+'" type="button" data-model-option="true" data-provider="'+esc(selection?.provider||'')+'" data-model="'+esc(selection?.model||'')+'" data-reasoning="'+esc(selection?.reasoningEffort||'')+'" data-default="'+(isDefault?'true':'false')+'"><span class="model-option-icon">'+(isDefault?I.brain:I.terminal)+'</span><span class="model-option-copy"><span class="model-option-name">'+esc(name)+'</span><span class="model-option-id">'+esc(id||'')+'</span></span><span class="model-option-check">'+I.check+'</span></button>'}
    function renderModelPicker(){const current=app.modelDirectory?.current,defaultModel=app.defaultModel||current,options=$('modelOptions');$('modelTriggerLabel').textContent=current&&defaultModel&&modelKey(current)!==modelKey(defaultModel)?modelDisplay(current):C.modelDefault;$('modelMenuHint').textContent=current?C.modelCurrent+': '+modelDisplay(current):C.modelLoadHint;let html=defaultModel?modelButton(defaultModel,C.modelDefault,modelDisplay(defaultModel),Boolean(current&&modelKey(current)===modelKey(defaultModel)),true):'<div class="model-option selected"><span class="model-option-icon">'+I.brain+'</span><span class="model-option-copy"><span class="model-option-name">'+esc(C.modelDefault)+'</span><span class="model-option-id">'+esc(C.modelLoadHint)+'</span></span><span class="model-option-check">'+I.check+'</span></div>';if(app.modelLoading)html+='<div class="model-menu-state">'+esc(C.modelLoading)+'</div>';else if(app.modelError)html+='<button id="modelRetry" class="model-menu-state error" type="button">'+esc(C.modelLoadingFailed)+'</button>';else if(app.modelDirectory){const groups=app.modelDirectory.groups||[];if(groups.length===0)html+='<div class="model-menu-state">'+esc(C.modelEmpty)+'</div>';else html+=groups.map(group=>{const items=(group.models||[]).filter(item=>!defaultModel||item.id!==defaultModel.model||group.id!==defaultModel.provider);return items.length?'<div class="model-group"><span class="model-group-name">'+esc(group.name||group.id)+'</span>'+items.map(item=>{const selection={provider:group.id,model:item.id};return modelButton(selection,item.name||item.id,item.id,Boolean(current&&current.provider===group.id&&current.model===item.id),false)}).join('')+'</div>':''}).join('');}options.innerHTML=html;document.querySelectorAll('[data-model-option="true"]').forEach(button=>{button.onclick=()=>{const provider=button.dataset.provider,model=button.dataset.model;if(!provider||!model)return;const reasoningEffort=button.dataset.reasoning;void selectModel({provider,model,...reasoningEffort?{reasoningEffort}:{}})}});const retry=$('modelRetry');if(retry)retry.onclick=()=>{app.modelError='';void loadModels()}}
    async function loadModels(){if(!app.activeSessionId||app.modelLoading)return;const sessionId=app.activeSessionId;app.modelLoading=true;app.modelError='';renderModelPicker();try{const payload=await api('/api/mobile/sessions/'+encodeURIComponent(sessionId)+'/models');if(app.activeSessionId!==sessionId)return;app.modelDirectory=payload;app.defaultModel=payload.current;app.modelLoading=false;renderModelPicker()}catch(error){if(app.activeSessionId!==sessionId)return;app.modelLoading=false;app.modelError=String(error.message||error);renderModelPicker()}}
    function toggleModelMenu(){const menu=$('modelMenu'),open=menu.hidden;menu.hidden=!open;$('modelTrigger').setAttribute('aria-expanded',open?'true':'false');if(open){renderModelPicker();if(!app.modelDirectory&&!app.modelLoading&&!app.modelError)void loadModels()}}
    async function selectModel(selection){if(!selection||app.modelSaving)return;if(app.modelDirectory?.current&&modelKey(selection)===modelKey(app.modelDirectory.current)){toggleModelMenu();return}app.modelSaving=true;renderModelPicker();try{const result=await api('/api/mobile/sessions/'+encodeURIComponent(app.activeSessionId)+'/model',{method:'POST',body:JSON.stringify(selection)});if(result.selected)app.modelDirectory={...app.modelDirectory,current:result.selected};$('modelMenu').hidden=true;$('modelTrigger').setAttribute('aria-expanded','false');showToast(C.modelSelected)}catch(error){showChatError(error)}finally{app.modelSaving=false;renderModelPicker()}}
    function showSessionList(){closeStream();app.activeSessionId=null;app.optimisticText='';resetModelPicker();document.body.classList.remove('chat-open');$('chatView').classList.remove('active');$('sessionsView').classList.add('active');if(history.state?.view==='chat')history.back();void refreshState({openRecent:false})}
    async function openSession(sessionId,title,pushHistory){if(!sessionId)return;closeStream();app.activeSessionId=sessionId;app.activeTitle=title||C.untitled;app.history={events:[],running:false};app.optimisticText='';resetModelPicker();$('chatTitle').textContent=app.activeTitle;$('messages').innerHTML='<div class="skeleton-list"><div class="skeleton"></div><div class="skeleton"></div><div class="skeleton"></div></div>';showChatError();document.body.classList.add('chat-open');$('sessionsView').classList.remove('active');$('chatView').classList.add('active');if(pushHistory)history.pushState({view:'chat',sessionId,title:app.activeTitle},'');openStream(sessionId);await loadHistory(sessionId,true)}
    function closeStream(){app.stream?.close();app.stream=null;clearTimeout(app.fallbackTimer);app.fallbackTimer=null}
    function openStream(sessionId){const source=new EventSource('/api/mobile/sessions/'+encodeURIComponent(sessionId)+'/stream');app.stream=source;source.addEventListener('snapshot',event=>{if(app.activeSessionId!==sessionId)return;applyHistory(JSON.parse(event.data),false);setConnection('connected')});source.addEventListener('disconnected',()=>location.replace('/'));source.onopen=()=>setConnection('connected');source.onerror=()=>{if(app.activeSessionId!==sessionId)return;setConnection('connecting');clearTimeout(app.fallbackTimer);app.fallbackTimer=setTimeout(()=>loadHistory(sessionId,false),1200)}}
    async function loadHistory(sessionId,stickToBottom){if(!sessionId||app.activeSessionId!==sessionId)return;try{const payload=await api('/api/mobile/sessions/'+encodeURIComponent(sessionId)+'/history');if(app.activeSessionId===sessionId)applyHistory(payload,stickToBottom)}catch(error){showChatError(error);setConnection('error')}}
    function applyHistory(payload,stickToBottom){app.history=payload||{events:[],running:false};const messages=visibleMessages(app.history.events||[]);if(app.optimisticText&&messages.some(message=>message.role==='user'&&message.blocks.some(block=>block.kind==='text'&&block.text===app.optimisticText)))app.optimisticText='';if(app.optimisticText)messages.push({role:'user',key:'optimistic',blocks:[{kind:'text',text:app.optimisticText}]});paintMessages(messages,stickToBottom);syncComposer()}
    function stepKey(data){return String(data.turn??'')+':'+String(data.step??'')}
    function contentBlocks(content){return(Array.isArray(content)?content:[]).flatMap(block=>{if(block?.type==='text'&&block.text)return[{kind:'text',text:block.text}];if(block?.type==='reasoning'&&block.text)return[{kind:'reasoning',text:block.text}];if(block?.type==='tool-call')return[{kind:'tool',id:block.id,name:block.name,args:block.arguments,status:'running'}];return[]})}
    function visibleMessages(events){const output=[],steps=new Map();function ensureStep(data){const key=stepKey(data);if(!steps.has(key)){const message={role:'assistant',key:key||'assistant-'+output.length,blocks:[],streaming:true};steps.set(key,message);output.push(message)}return steps.get(key)}for(const entry of events){const event=entry?.event||entry||{},data=event.data||{},type=String(event.type||'').toLowerCase();if(type==='user/message'){const message=data.message||data,blocks=contentBlocks(message.content).filter(block=>block.kind==='text');if(blocks.length)output.push({role:'user',key:'user-'+event.seq,blocks});continue}if(type==='assistant/chunk'){const chunk=data.chunk||{};if(chunk.type!=='text-delta'&&chunk.type!=='reasoning-delta')continue;const message=ensureStep(data),kind=chunk.type==='text-delta'?'text':'reasoning',streamKey=kind+':'+String(chunk.index??0);let block=message.blocks.find(item=>item.streamKey===streamKey);if(!block){block={kind,text:'',streamKey};message.blocks.push(block)}block.text+=chunk.text||chunk.delta||'';continue}if(type==='assistant/message'){const message=ensureStep(data);message.blocks=contentBlocks(data.message?.content);message.streaming=false;continue}if(type==='tool/call'){const message=ensureStep(data);if(!message.blocks.some(block=>block.kind==='tool'&&block.id===data.callId))message.blocks.push({kind:'tool',id:data.callId,name:data.name,args:data.arguments,status:'running'});continue}if(type==='tool/result'){const message=ensureStep(data),result=(data.message?.content||[]).find(block=>block?.type==='tool-result'),id=result?.toolCallId;let tool=message.blocks.find(block=>block.kind==='tool'&&block.id===id);if(!tool){tool={kind:'tool',id,name:id||'tool',args:'',status:'running'};message.blocks.push(tool)}tool.status=data.error||result?.isError?'error':'done';tool.result=(result?.content||[]).filter(block=>block?.type==='text').map(block=>block.text).join('\\n');message.streaming=false}}return output.filter(message=>message.blocks?.some(Boolean))}
    function inlineMarkdown(text){return esc(text).replace(/\x60([^\x60]+)\x60/g,'<code>$1</code>').replace(/\\*\\*([^*]+)\\*\\*/g,'<strong>$1</strong>').replace(/\\[([^\\]]+)\\]\\((https?:\\/\\/[^ )]+)\\)/g,'<a href="$2" target="_blank" rel="noreferrer">$1</a>')}
    function markdown(text){const lines=String(text||'').split('\\n');let html='',code=false,list=false;for(const line of lines){if(line.trim().startsWith(String.fromCharCode(96,96,96))){if(list){html+='</ul>';list=false}html+=code?'</code></pre>':'<pre><code>';code=!code;continue}if(code){html+=esc(line)+'\\n';continue}const heading=line.match(/^(#{1,3})\\s+(.+)$/);if(heading){if(list){html+='</ul>';list=false}const level=heading[1].length;html+='<h'+level+'>'+inlineMarkdown(heading[2])+'</h'+level+'>';continue}const bullet=line.match(/^[-*]\\s+(.+)$/);if(bullet){if(!list){html+='<ul>';list=true}html+='<li>'+inlineMarkdown(bullet[1])+'</li>';continue}if(list){html+='</ul>';list=false}if(line.trim())html+='<p>'+inlineMarkdown(line)+'</p>'}if(list)html+='</ul>';if(code)html+='</code></pre>';return html}
    function pretty(value){if(!value)return'';try{return JSON.stringify(typeof value==='string'?JSON.parse(value):value,null,2)}catch{return String(value)}}
    function reasoningSummary(text){return String(text||'').split('\\n').map(line=>line.trim()).filter(Boolean)[0]||C.running}
    function renderBlock(block,streaming,key){if(block.kind==='text')return'<div class="markdown'+(streaming?' streaming':'')+'">'+markdown(block.text)+'</div>';if(block.kind==='reasoning')return'<details class="activity" data-state="'+(streaming?'running':'done')+'" data-activity-key="reasoning-'+esc(key)+'"><summary><span class="activity-icon">'+I.brain+'</span><span class="activity-title">'+esc(C.thinking)+'</span><span class="activity-dot"></span><span class="activity-summary">'+esc(reasoningSummary(block.text))+'</span><span class="activity-chevron">'+I.chevronRight+'</span></summary><div class="activity-body markdown">'+markdown(block.text)+'</div></details>';if(block.kind==='tool'){const label=block.status==='running'?C.toolRunning:block.status==='error'?C.toolFailed:C.toolDone;return'<details class="activity" data-state="'+esc(block.status)+'" data-activity-key="tool-'+esc(block.id||key)+'"><summary><span class="activity-icon">'+I.terminal+'</span><span class="activity-title">'+esc(block.name||'tool')+'</span><span class="activity-dot"></span><span class="activity-summary">'+esc(label)+'</span><span class="activity-chevron">'+I.chevronRight+'</span></summary><div class="activity-body">'+(block.args?'<strong>'+esc(C.input)+'</strong><pre>'+esc(pretty(block.args))+'</pre>':'')+(block.result?'<strong>'+esc(C.output)+'</strong><pre>'+esc(block.result)+'</pre>':'')+'</div></details>'}return''}
    function renderMessage(message){if(message.role==='user')return'<div class="message user">'+message.blocks.map(block=>esc(block.text)).join('\\n')+'</div>';return'<div class="message assistant">'+message.blocks.map((block,index)=>renderBlock(block,message.streaming&&index===message.blocks.length-1,message.key+'-'+index)).join('')+'</div>'}
    function paintMessages(messages,stickToBottom){const host=$('messages'),nearBottom=host.scrollHeight-host.scrollTop-host.clientHeight<100,openActivities=new Set([...host.querySelectorAll('details[open][data-activity-key]')].map(item=>item.dataset.activityKey)),empty=messages.length===0?'<div class="empty"><span class="empty-mark">'+I.comment+'</span><strong>'+esc(C.emptyConversation)+'</strong></div>':'';host.innerHTML=messages.map(renderMessage).join('')+(app.history.running?'<div class="turn-status" role="status">'+esc(C.running)+'…</div>':'')+empty;host.querySelectorAll('details[data-activity-key]').forEach(item=>{if(openActivities.has(item.dataset.activityKey))item.open=true});if(stickToBottom||nearBottom)host.scrollTop=host.scrollHeight}
    function syncComposer(){const running=app.history.running||app.sending;$('send').hidden=running;$('cancel').hidden=!running;$('prompt').disabled=running;$('chatState').textContent=running?C.running:C.connected}
    function resizePrompt(){const input=$('prompt');input.style.height='40px';input.style.height=Math.min(150,Math.max(40,input.scrollHeight))+'px';$('send').disabled=!input.value.trim()||app.sending}
    async function sendPrompt(){if(!app.activeSessionId||app.sending)return;const input=$('prompt'),text=input.value.trim();if(!text)return;app.sending=true;app.optimisticText=text;app.history.running=true;input.value='';resizePrompt();applyHistory(app.history,true);try{await api('/api/mobile/sessions/'+encodeURIComponent(app.activeSessionId)+'/prompt',{method:'POST',body:JSON.stringify({text})});showChatError()}catch(error){app.optimisticText='';app.history.running=false;input.value=text;resizePrompt();showChatError(error)}finally{app.sending=false;syncComposer()}}
    async function cancelSession(){if(!app.activeSessionId)return;try{await api('/api/mobile/sessions/'+encodeURIComponent(app.activeSessionId)+'/cancel',{method:'POST',body:'{}'});app.history.running=false;syncComposer();await loadHistory(app.activeSessionId,false)}catch(error){showChatError(error)}}
    function syncViewport(){const viewport=window.visualViewport;document.documentElement.style.setProperty('--app-height',(viewport?.height||window.innerHeight)+'px');if(document.activeElement===$('prompt'))requestAnimationFrame(()=>$('prompt').scrollIntoView({block:'end'}))}
    $('workspace').onchange=renderSessionRows;$('newSession').onclick=createSession;$('refresh').onclick=()=>refreshState({visible:true,openRecent:false});$('back').onclick=showSessionList;$('modelTrigger').onclick=toggleModelMenu;$('composer').onsubmit=event=>{event.preventDefault();void sendPrompt()};$('cancel').onclick=cancelSession;$('prompt').oninput=resizePrompt;$('prompt').onkeydown=event=>{if(event.key==='Enter'&&!event.shiftKey&&!event.isComposing&&event.keyCode!==229){event.preventDefault();void sendPrompt()}};$('prompt').onfocus=syncViewport;
    history.replaceState({view:'sessions'},'');window.onpopstate=event=>{if(event.state?.view==='chat'&&event.state.sessionId)void openSession(event.state.sessionId,event.state.title,false);else if(app.activeSessionId)showSessionList()};document.addEventListener('visibilitychange',()=>{if(!document.hidden){void refreshState({openRecent:false});if(app.activeSessionId&&!app.stream)openStream(app.activeSessionId)}});window.visualViewport?.addEventListener('resize',syncViewport);window.visualViewport?.addEventListener('scroll',syncViewport);window.addEventListener('resize',syncViewport);syncViewport();resizePrompt();void refreshState({openRecent:true});
  </script>
</body>
</html>`
}

export function renderPairingPage(requestId: string, locale: MobilePageLocale = 'zh'): string {
  const zh = locale === 'zh'
  const copy = {
    title: zh ? '连接 EzDSH' : 'Connect EzDSH',
    heading: zh ? '批准此手机' : 'Approve this phone',
    hint: zh ? '请回到电脑，在远程控制设置中确认本次连接。' : 'Return to the desktop and approve this connection in Remote Control settings.',
    waiting: zh ? '正在等待电脑端批准…' : 'Waiting for desktop approval…',
    connected: zh ? '连接成功，正在打开会话…' : 'Connected. Opening sessions…',
    rejected: zh ? '电脑端拒绝了本次连接。' : 'The desktop declined this connection.',
    expired: zh ? '连接申请已过期，请重新生成二维码。' : 'This request expired. Generate a new QR code.',
    unavailable: zh ? '暂时无法获取配对状态。' : 'Pairing status is temporarily unavailable.',
  }
  return `<!doctype html><html lang="${zh ? 'zh-CN' : 'en'}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#ffffff" media="(prefers-color-scheme:light)"><meta name="theme-color" content="#141416" media="(prefers-color-scheme:dark)"><link rel="icon" href="/app-icon"><link rel="apple-touch-icon" href="/app-icon"><title>${copy.title}</title><style>:root{color-scheme:light;--bg:#fff;--surface:#f6f7f8;--ink:#18191c;--muted:#858990;--line:#e5e7eb;--brand:#4d6bfe;--danger:#df4754}@media(prefers-color-scheme:dark){:root{color-scheme:dark;--bg:#141416;--surface:#202023;--ink:#f5f5f6;--muted:#9799a0;--line:#303034;--brand:#7188ff;--danger:#ff6b78}}*{box-sizing:border-box}body{min-height:100dvh;margin:0;padding:24px;display:flex;align-items:center;justify-content:center;background:var(--bg);color:var(--ink);font:15px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.card{width:100%;max-width:350px;text-align:center}.logo{width:60px;height:60px;margin:0 auto 22px;border-radius:17px;box-shadow:0 12px 30px rgba(0,0,0,.12)}h1{margin:0 0 8px;font-size:25px;line-height:1.2;letter-spacing:-.03em}p{margin:0;color:var(--muted)}.waiting{display:flex;justify-content:center;gap:7px;margin:27px 0 20px}.waiting i{width:7px;height:7px;border-radius:50%;background:var(--brand);animation:p 1s infinite alternate}.waiting i:nth-child(2){animation-delay:.2s}.waiting i:nth-child(3){animation-delay:.4s}.waiting.stopped i{background:var(--muted);animation:none}.state{padding:12px 14px;border-radius:12px;background:var(--surface);font-size:13px}.state.error{color:var(--danger)}@keyframes p{to{opacity:.2;transform:translateY(-3px)}}@media(prefers-reduced-motion:reduce){*{animation:none!important}}</style></head><body><main class="card"><img class="logo" src="/app-icon" alt=""><h1>${copy.heading}</h1><p>${copy.hint}</p><div id="waiting" class="waiting"><i></i><i></i><i></i></div><div id="state" class="state">${copy.waiting}</div></main><script>const id=${safeScriptJson(requestId)},state=document.getElementById('state'),waiting=document.getElementById('waiting'),C=${safeScriptJson(copy)};async function poll(){try{const response=await fetch('/pair/status?id='+encodeURIComponent(id),{credentials:'same-origin',cache:'no-store'});const data=await response.json();if(data.status==='approved'){state.textContent=C.connected;location.replace(data.redirect||'/');return}if(data.status==='rejected'){state.textContent=C.rejected;state.classList.add('error');waiting.classList.add('stopped');return}if(!response.ok)throw new Error(data.error||C.unavailable)}catch(error){state.textContent=error.message||C.unavailable;state.classList.add('error');waiting.classList.add('stopped');return}setTimeout(poll,900)}poll();</script></body></html>`
}

export function renderMobileLandingPage(locale: MobilePageLocale = 'zh'): string {
  const zh = locale === 'zh'
  const copy = {
    title: zh ? '连接 EzDSH' : 'Connect EzDSH',
    heading: zh ? '需要重新连接' : 'Reconnect this phone',
    hint: zh ? '请在电脑端打开“远程控制”，生成新的二维码并批准此手机。' : 'Open Remote Control on the desktop, generate a new QR code, and approve this phone.',
  }
  return `<!doctype html><html lang="${zh ? 'zh-CN' : 'en'}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#ffffff" media="(prefers-color-scheme:light)"><meta name="theme-color" content="#141416" media="(prefers-color-scheme:dark)"><link rel="icon" href="/app-icon"><title>${copy.title}</title><style>:root{color-scheme:light;--bg:#fff;--surface:#f6f7f8;--ink:#18191c;--muted:#858990}@media(prefers-color-scheme:dark){:root{color-scheme:dark;--bg:#141416;--surface:#202023;--ink:#f5f5f6;--muted:#9799a0}}*{box-sizing:border-box}body{min-height:100dvh;margin:0;padding:24px;display:flex;align-items:center;justify-content:center;background:var(--bg);color:var(--ink);font:15px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.card{width:100%;max-width:350px;text-align:center}.logo{width:60px;height:60px;margin:0 auto 22px;border-radius:17px;box-shadow:0 12px 30px rgba(0,0,0,.12)}h1{margin:0 0 8px;font-size:25px;letter-spacing:-.03em}p{margin:0;padding:13px 15px;border-radius:13px;background:var(--surface);color:var(--muted);font-size:13px}</style></head><body><main class="card"><img class="logo" src="/app-icon" alt=""><h1>${copy.heading}</h1><p>${copy.hint}</p></main></body></html>`
}

function renderIcon(definition: IconDefinition): string {
  return icon(definition, { attributes: { 'aria-hidden': 'true', focusable: 'false' } }).html.join('')
}

function safeScriptJson(value: unknown): string {
  return JSON.stringify(value).replace(/</gu, '\\u003c')
}
